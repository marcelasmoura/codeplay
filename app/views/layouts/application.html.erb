<!DOCTYPE html>
<html>
  <head>
    <title>Codeplay</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <div class="menu-navegacao">
      <nav>
        <% if user_signed_in? %>
          <%= current_user.email %>
          <%= link_to("Sair", "#") %>
        <% else %>
          <%= link_to("Registrar-se", new_user_session_path) %>
        <% end %>
      </nav>
    </div>

    <div class='container'>
      <%= flash[:alert] %>
      <%= yield %>
    </div>
  </body>
</html>
